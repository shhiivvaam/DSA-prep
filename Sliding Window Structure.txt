int left = 0;
int right = 0;
int n = arr.size();

int ans = 0;
int count = 0;

for(right = 0;right<n;right++) {
	for(left = 0;(condition); left++) {
		
	}
	ans = Math.max(ans, right-left+1);
}
return ans-1;